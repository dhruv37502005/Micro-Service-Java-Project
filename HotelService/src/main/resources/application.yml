server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/HotelServices
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true